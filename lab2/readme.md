#crud
c----creat/insert/add
r---retrive/view/gate
u---update/edit
d---delete/remove
fs---file system(nodes library which does not comunicate direct os se)
it direct connect with client os rater than browser
## major task of fs module
-reading and writting files 
      -readfiles
      -writefiles
      -appendfile
      ---directry management
      -mkdir()
      -rmdir()

      -fstat()
      ---watching for change
      -watch()
      -watchfiles()
      -unwatchfiles()
      ---strimming large files
      -creatreadsrim()
      creawritesrim()
      ---file oprators
      -rename()
      -truncate()
      -unlink()
      -link()
      -syslink()
      --meta data informTION
      stat()
      lsstat()
      fstat()

